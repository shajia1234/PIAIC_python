DEl just delete krega
POP se use bh kr skte hen i.e. to save somewhere
LIST me [] use krte hen or editable h
TUPLES are not editable and use ()
to edit a tuple , it can be transformes into LISt by typecasting
RANGE is used for numbers only

code:
idx = 1
for student_age in student_ages:
    for i range(idx , len(student_ages)):
        if student_age > student_ages[i]:
            temp = student_age
            student_ages[i - 1] = studeent_ages[i]
            student_ages[i] = temp
    idx += 1
print(student_ages)